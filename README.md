# Monmart | Pokemon TCG Marketplace

## Installation

Dependencies need to be added using `npm install` in not only the frontend and backend folders, but also the overarching repository&mdash;installing the package, `concurrently` which allows one terminal to run one start script to run the React app and server.
